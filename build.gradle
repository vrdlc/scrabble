apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "scrabble"
version = '1.0'
mainClassName = "Scrabble"

repositories {
  mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
